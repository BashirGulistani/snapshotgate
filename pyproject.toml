[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "snapshotgate"
version = "0.1.0"
description = "Lightweight data contract + drift gate for CSV/JSONL."

